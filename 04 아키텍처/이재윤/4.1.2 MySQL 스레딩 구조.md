

```
## Summary

1) MySQL 서버는 프로세스 기반이 아니라, 스레드 기반으로 작동하며,
   크게 포그라운드(Foreground) 스레드와 백그라운드(Background) 스레드로 구분할 수 있음

2) 포그라운드 스레드(클라이언트 스레드)
- 포그라운드 스레드는 최소한 MySQL 서버에 접속된 클라이언트의 수만큼 존재함
-> 주로 각 클라이언트 사용자가 요청하는 쿼리 문장을 처리함
-> 클라이언트 사용자가 작업을 마치고 커넥션을 종료하면
   해당 커넥션을 담당하던 스레드는 다시 스레드 캐시(Thread cache)로 되돌아감


3) 백그라운드 스레드
- InnoDB는 다음과 같이 여러 가지 작업이 백그라운드로 처리됨
(1) 인서트 버퍼(Insert Buffer)를 병합하는 스레드
(2) 로그를 디스크로 기록하는 스레드
(3) InnoDB 버퍼 풀의 데이터를 디스크에 기록하는 스레드
(4) 데이터를 버퍼로 읽어 오는 스레드
(5) 잠금이나 데드락을 모니터링하는 스레드

- 모두 중요한 역할을 하지만, 그중에서도 가장 중요한 것은 로그 스레드(Log thread)와
  버퍼의 데이터를 디스크로 내려쓰는 작업을 처리하는 쓰기 쓰레드(Write thread)일 것임

- 사용자의 요청을 처리하는 도중 데이터의 쓰기 작업은 지연(버퍼링)뙤어 처리될 수 있지만,
  데이터의 읽기 작업은 절대 지연될 수 없음
-> 그래서 일반적인 상용 DBMS에는 대부분 쓰기 작업을 버퍼링해서 일괄 처리하는 기능이 탑재돼 있으며,
   InnoDB 또한 이러한 방식으로 처리됨 


## Insight
- MySQL 스레드가 포그라운드 스레드와 백그라운드 스레드로 나뉜다는 점
-> 그리고 백그라운드 스레드가 여러 종류로 나뉘며, 각각이 중요한 기능을 한다는 점


## Application
- MySQL 스레드를 분석해서 문제가 발생했을 때, 문제를 파악하고 해결할 수 있는 역량을 길러보고 싶다.

```
![image](https://github.com/LeeJaeYun7/real-mysql/assets/134198681/10fd56f4-eac4-48ee-b146-8b7bc64f4c7b)

